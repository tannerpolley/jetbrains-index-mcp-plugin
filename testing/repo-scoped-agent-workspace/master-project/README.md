# Master Project

Open this directory as one IntelliJ project window.

The three child folders are intended to be separate Git repos loaded together:

- `inventory-repo`
- `billing-repo`
- `analytics-repo`

Each repo repeats the shared probe names so repo-scope leaks are easy to detect.
